# Progress Tracker
Update this file whenever the current phase, active feature, or implementation state changes.
## Current Phase
- Feature 37 create-and-manage course tracks is implemented. Active lecturers can create graph-safe lecturer-owned aggregates with a private DRAFT track, DRAFT version 1, validated configuration, closed enrollment, stable collision-safe slug, trusted owner identity, and `OWNS_TRACK` proof. Typed course contracts and safe projections cover create/update/configuration inputs, summary/detail/version/configuration views, readiness blockers, available actions, and bounded list results; raw graph nodes are not returned to the lecturer UI.
- Feature 37 management adds centrally validated metadata/configuration changes, explicit nullable-field clearing, immutable published-version protection, optimistic revisions with `COURSE_REVISION_CONFLICT`, bounded owner-scoped search/filter/sort pagination, archival without deletion, unpublished-draft restore with enrollment kept closed, and idempotent course duplication. Creation idempotency is scoped to actor/operation/request key, persists a payload fingerprint, replays the original aggregate, and rejects changed payloads with `IDEMPOTENCY_KEY_PAYLOAD_CONFLICT`.
- Feature 37 authorization and transport are integrated through the Feature 36 trusted actor/policy boundary. Eight private thin walkers expose create/get/list/update/configuration/archive/restore/duplicate operations; corresponding typed private RPC functions support the client. The lecturer UI now has authenticated `/lecturer/courses`, `/lecturer/courses/new`, and `/lecturer/courses/:trackId` routes with a bounded owned-course list, private-draft creation form, draft banner, overview/setup tabs, readiness display, and locked placeholders for later source/curriculum/publishing features.
- Feature 37 verification passes: project-local `.jac/venv/Scripts/jac.exe check main.jac` passes with the new server, walkers, and client routes; the focused course-management suite passes 4/4 tests covering private-draft aggregate integrity, trusted ownership and required version/configuration topology, idempotent replay and changed-payload conflict, optimistic revision conflict, and archive/restore lifecycle behavior. Runs emit only informational schema-drift messages for older locally persisted `LearningTrack` records after the additive revision fields.
- Feature 37 lint reports zero errors across the nine changed/new Feature 37 modules. Production bundling remains blocked by the existing Windows Bun cache permissions problem: `jac build main.jac` reaches dependency installation, then reports widespread `EPERM` cache/package directory failures and missing nested dependency folders. The repository has no root `package.json`, so `npm run build` is not an applicable build path.
- Feature 36 centralized lecturer authorization policies are implemented. Added typed lecturer/admin permissions, decision/scope/reason enums, collaborator-ready access roles, trusted actor and resource contexts, safe public error mapping, and a deterministic default-deny policy service. Role/profile state is rebuilt from the authenticated root for each use, and no final authorization-decision cache is used, so role suspension/revocation and resource-state changes take effect on the next evaluation.
- Feature 36 resource authorization validates scalar plus `OWNS_TRACK` ownership, detects contradictory edges, denies lecturer access to system tracks, supports feature-flagged/metric-counted scalar fallback only for additive migration, and supplies repository-built track/version/document/blueprint/enrollment/submission contexts that prove their graph relationships and same-track/version scope. A private idempotent ownership-backfill operation adds only missing non-conflicting edges and reports conflicts without guessing.
- The first-release publication model is now explicit: owners/editors may edit and submit; reviewer or administrator approval is required by default; owner self-approval defaults off; owner publishing requires both server-controlled publish capability and `ALLOW_LECTURER_SELF_PUBLISH`. Published and superseded versions remain immutable even for administrators.
- Authorization is integrated through trusted lecturer course adapters for create/next-draft/publication, policy-filtered private owned-track listing, status-aware own-profile reads/edits, explicit ADMIN permission evaluation for lecturer activation and programming-track migration/rollback, and corrected `ActorContext.lecturer_id` ownership in track creation/visibility. The complete current/planned privileged-operation audit is recorded in `context/migration-inventories/36-lecturer-authorization-policies.md`.
- Feature 36 verification passes: all authorization domain, actor, repository, policy, metric, application-adapter, private-service, test, and `main.jac` checks pass; the negative-first authorization suite passes 9/9; Feature 35 identity regressions pass 5/5; Feature 33 track repository/service regressions pass 5/5; and lint reports zero errors across the eight new authorization modules. Tests cover default deny, fake role claims, pending/suspended/archived profiles, cross-owner denial, scalar/edge conflict, strict observable fallback, editor/reviewer/system scopes, published immutability for owners/admins, course-scoped submission review, explicit admin permissions, safe not-found mapping, audit capture, and graph-proven ownership.
- Feature 35 user roles and lecturer profiles is implemented. Authentication remains Jac Scale's single credential/session system, while `jid(root)` is now the stable server-derived application user ID. The previous client-supplied `auth_user_id` authority was removed; email, username, cached roles, and workspace choices are non-authoritative snapshots only.
- Feature 35 adds typed learner/lecturer/admin role assignments with pending/active/suspended/revoked lifecycle and source/audit timestamps; root-owned identity, role, learner-profile, and lecturer-profile relationships; a persistent lecturer profile with verification/onboarding state, server-calculated completion, protected capabilities, and validated lifecycle transitions; bounded identity/lecturer repositories; centralized actor, role, lecturer, onboarding, and course-owner services; structured identity events; and the required stable domain error contracts.
- Existing login/registration now idempotently preserves or creates the existing `Learner`, creates the canonical learner edge and ACTIVE LEARNER assignment, and exposes active role plus lecturer identifiers to the client for role-aware navigation. Lecturer requests default to `ADMIN_APPROVAL`; `INVITATION_ONLY` denies public requests and `DEVELOPMENT_AUTO_ACTIVATE` must be explicitly configured. Publishing capability also defaults off unless explicitly configured.
- Feature 35 includes an idempotent current-root learner-role migration with MIGRATED/UNCHANGED/SKIPPED/CONFLICT reporting, identity graph conflict blocking, ADMIN-protected lifecycle scaffolding, lecturer-aware track ownership comparison, a documented authentication audit in `context/identity-auth-audit.md`, and focused tests for completion, lifecycle validity, capability removal, safe update inputs, and active-role resolution.
- Feature 35 verification passes: `jac check main.jac` and all new/changed server identity modules pass; focused identity tests pass 5/5; the existing track repository/service regression suite passes 5/5 after lecturer-owner comparison moved to `ActorContext.lecturer_id`; and `jac lint` reports zero errors across the five new identity domain/service/test modules. The project-local compiler at `.jac/venv/Scripts/jac.exe` was used because `jac` is not on the shell PATH.
- The current post-expansion account-flow issues are fixed. Dashboard loading no longer supplies assessment-journey values from browser storage as authoritative roadmap/language filters, preventing a newly authenticated account from inheriting another account's stale roadmap id; a learner without a roadmap now receives the existing normal `ready` dashboard projection with an onboarding state such as `no_assessment`.
- Assessment-to-roadmap handoff now forwards track/version identifiers from the client and, independently, derives authoritative track context from the persisted learner-owned assessment on the server. Missing client context is repaired from the assessment, conflicting explicit context is rejected, and generated roadmaps retain the assessment's normalized track/version identifiers. This removes the new-account `Unable to resolve learning track` 500.
- Scale compatibility now includes an explicit public `GET /functions` discovery endpoint in `services/system.sv.jac`, while callable application functions remain at `POST /function/<name>` and production Swagger remains disabled. This addresses the installed Scale server's omission of the core function-listing route; requests through the development UI port still require the Jac backend to be running because port 8001 is only the client/proxy surface.
- Current issue verification passed: all 8 affected modules/tests and `main.jac` pass `jac check`; function discovery passes 1/1 tests, dashboard service passes 4/4 tests, and roadmap generation/handoff passes 7/7 tests. Faux route introspection lists `GET /functions` and 78 total endpoints, then exits nonzero only because the installed Scale/Jac faux shutdown calls the missing `JFastApiServer.server_close` method. Existing accumulated SQLite schema-drift notices remain informational.
- Post-Feature 34 issue fixes are implemented. Legacy roadmaps without persisted track identifiers now seed the deterministic built-in catalogue before lesson/challenge track resolution, pass their available parent context into generation, and persist the resolver's normalized track/version identifiers on newly generated content. This removes the returning-learner `BUILTIN_TRACK_NOT_SEEDED` / "registered track graph is incomplete" lesson failure while retaining legacy language fallback.
- New-account assessment selection now carries the server-issued `track_id` and `track_version_id` from the supported-track catalogue through the client callback into `start_assessment_journey`, while retaining the legacy language value for compatibility. This removes the `Unsupported assessment language` 500 caused by dropping canonical track context at the UI boundary.
- Post-Feature 34 verification passed: the affected lesson/challenge walkers, lesson service, assessment page, both focused test modules, and `main.jac` pass `jac check`; all 7 lesson-generation tests and all 8 assessment-journey tests pass, including new legacy-roadmap seeding and new-account track-first regressions. Runs still emit pre-existing best-effort SQLite schema-drift notices from accumulated local graph data.
- Feature 34 track-aware walker compatibility migration is implemented. Added the shared `lib/learning_track/context_resolver.jac` contract with explicit, parent-entity, enrollment, active-version, and legacy-language resolution; normalized typed track metadata; parent precedence; stable mismatch/conflict errors; learner ownership checks; and relationship-proven track/version validation over the Feature 33 repository boundary.
- Feature 34 walker contracts are additive across assessment, roadmap, lesson, challenge, submission, mastery, progression, dashboard, skill-map, and recommendation flows. Existing names, legacy language inputs, and report consumers remain compatible; assessment/roadmap/content/evaluation mutations now resolve authoritative track context before their existing behavior, generated lesson/challenge views expose track/version IDs, and programming-only generators return stable unsupported-activity errors for non-programming tracks instead of changing track identity.
- Feature 34's audited walker/call-site inventory is recorded in `context/migration-inventories/34-track-aware-walker-migration.md`. Client journey recovery continues to persist `selectedTrackId` and `selectedTrackVersionId` centrally while retaining the temporary language key.
- Feature 34 verification passed: the resolver, all 13 affected walker modules, the focused resolver test module, and `main.jac` pass `jac check`. All 3 focused resolver tests pass, covering explicit and legacy normalization, parent-entity precedence, track conflict rejection, and learner-spoofing rejection. The 6-test roadmap regression suite also passes after preserving its legacy `unsupported_language` error contract. Test output includes pre-existing local SQLite schema-drift notices from accumulated graph data; no final test run failed.
- Feature 33 track repositories and services is implemented. Added shared repository/service errors and results, bounded pagination, trusted `ActorContext`, `IdempotencyContext`, catalogue/progress projections, and durable `ApplicationOperation` markers for completed or detectable incomplete multi-step mutations.
- Feature 33 focused graph repositories centralize catalogue membership, authorization-safe visibility/ownership lists, relationship-proven track-version lookup, version-scoped module/skill/prerequisite/lesson/activity queries, deterministic ordering and prerequisite-cycle detection, plus enrollment-rooted assessment, roadmap, submission, mastery, lesson-progress, and recommendation reads. Published-version writes are rejected and learner/curriculum queries cannot silently cross enrollment/version boundaries.
- Feature 33 services centralize legacy track-context fallback, track management authorization, published-version immutability, idempotent track creation with initial version/configuration, single-draft enforcement, publication preflight/activation/repair-state handling, enrollment creation with authoritative graph edges, and enrollment-scoped progress projections. Feature 34 remains responsible for migrating UI-facing walkers to these services, consistent with Feature 33's staged adoption plan and no-breaking-change constraint.
- Feature 33 verification passed: shared types, repositories, application services, the focused test module, and `main.jac` pass `jac check`; `jac lint` reports zero errors across the four Feature 33 Jac modules; all 5 focused tests pass, covering relationship-proven version ownership, cross-version curriculum isolation, deterministic prerequisite-cycle detection, centralized legacy resolution and mismatch rejection, trusted-owner/idempotent creation, and published-version immutability. The repository-wide bare `jac test` command still stops on the pre-existing client import error `No module named 'frontend.cl'; 'frontend' is not a package`; the Feature 33 suite itself is green.
- Feature 32 learning-track OSP schema is implemented as the graph foundation for Features 33 and 34. Added version-scoped shared curriculum/source nodes (`CourseModule`, `LessonTemplate`, `ActivityTemplate`, `CourseDocument`, `CourseContentChunk`, and `CourseBlueprint`), learner-scoped `Enrollment`, `RoadmapItem`, and generalized `Submission` nodes, and compatibility-safe schema mirrors on the existing `Skill`, `AssessmentAttempt`, `Roadmap`, `LessonProgress`, `Mastery`, and `Recommendation` archetypes. Existing constructors and programming-track flows remain backward compatible.
- Feature 32's complete named relationship vocabulary is declared in `edges/domain_edges.jac`, covering catalogue, version ownership, curriculum composition, prerequisites, source traceability, enrollment, roadmap, progress, submission, mastery, evidence, and recommendation topology. The domain barrel exports the new nodes, edges, integrity DTOs, unlock DTO, and learner-safe skill projection. `lib/learning_track/graph_schema.jac` establishes stable cross-track, cross-version, self-prerequisite, and prerequisite-bound validation errors for reuse by the Feature 33 repository/service layer.
- Feature 32 verification passed: `jac check` succeeded for `main.jac` and all 13 changed schema/domain/test modules. The focused OSP suite has 4 passing tests covering named curriculum reachability, enrollment-scoped roadmap topology, cross-track/version isolation, and prerequisite self-link/value rejection. The existing Feature 31 compatibility suite also remains green with 4 passing tests. Full repository-coordinated duplicate detection, cycle traversal, catalogue/enrollment attachment, migration repair, authorization, and administrative integrity walkers remain assigned to Features 33 and 34 rather than being embedded in schema declarations.
- Feature 31 track-aware existing models is implemented at the model boundary. Existing Feature 30 dual-written assessment, roadmap, generated lesson/challenge, submission/evaluation, mastery, and progression records retain safe optional `track_id`/`track_version_id` defaults; `Learner` now also exposes explicit `active_track_id`/`active_track_version_id` fields while retaining deprecated compatibility aliases, and shared `Skill` records can identify their owning track version.
- Feature 31 optional persisted projections and centralized integrity contracts are implemented. Added track-aware `Recommendation`, `SkillMapSnapshot`, and `DashboardSnapshot` nodes plus `TrackValidationResult`; `lib/learning_track/validation.jac` now provides shared track existence, version existence/ownership, parent-child matching, curriculum/learner record validation, resolution, and authoritative-parent copying with stable migration error codes. The existing dynamic dashboard, skill-map, and tutor responses remain non-persisted and unchanged.
- Feature 31 verification passed: the focused model suite has 4 passing tests covering legacy-safe defaults, track/version ownership, authoritative parent copying, cross-track mismatch rejection, and isolated persisted projection context. `jac check main.jac` passed, the focused test module passed static checking, and lint completed with zero errors across all changed Jac modules. Track-aware repositories/services and walker input/write enforcement remain scoped to Features 33 and 34, consistent with Feature 31's non-goals.
- Feature 30 programming-track migration is implemented. Python and Jaseci now come from one typed canonical registry with deterministic track/version/configuration IDs and exact legacy aliases; seed operations explicitly report created, unchanged, repaired, skipped, and failed outcomes and reject reserved-ID collisions.
- Feature 30 compatibility and persistence migration are implemented. Learner, assessment, roadmap, generated lesson/challenge, submission/evaluation, mastery, and progression records carry optional track/version references; all active programming flows resolve track-first and dual-write canonical and legacy context. The migration supports dry-run/apply, bounded roadmap batches, learner scoping, conflict preservation, structured observability, durable per-entity rollback records, and an environment-gated authenticated endpoint. The frontend persists canonical selected track/version keys while retaining legacy language recovery.
- Feature 30 inventory and verification artifacts are in `context/migration-inventories/30-programming-track-migration.md` and `lib/learning_track/tests/programming_track_migration_test.jac`. Static Jac checks for the changed domain, services, walkers, client journey state, migration module, and entrypoint pass. All 6 focused migration tests pass, covering registry validation, exact alias/conflict resolution, seed idempotency, mutation-free dry runs, related-record backfill, repeat-safe apply, rollback, and conflict preservation. Local runs emit existing SQLite schema-drift notices from accumulated pre-migration data; no test failures occurred.
- Feature 29 learning-track domain model is implemented. Added controlled track/version/configuration enums and graph nodes, named catalogue/version/configuration edges, stable UUID-backed lecturer track IDs, validated collision-safe slugs, typed summary/detail/error/result contracts, centralized ownership/lifecycle/version validation, immutable publication activation, discovery/enrollment rules, structured domain events, and a deployment-wide `root.shared` catalogue boundary that remains separate from learner progression.
- Feature 29 built-in compatibility is implemented. Python and Jaseci are deterministic published version-1 tracks (`trk_builtin_python`, `trk_builtin_jaseci`) with idempotent configurations/catalogue/version relationships; the assessment track list now exposes those stable IDs while preserving its legacy `Python`/`Jac` selection values. The legacy resolver trims and normalizes `python`, `jac`, and `jaseci`, gives `track_id` precedence, and rejects unknown or missing inputs without auto-creating curricula. Typed `/function/list_learning_tracks`, `/function/get_learning_track`, `/function/resolve_legacy_learning_track`, and authenticated migration endpoints are registered through `main.jac`.
- Feature 29 verification: `jac check` passed for the new domain node, service, typed API, domain barrel, assessment journey integration, focused tests, and `main.jac`. `jac lint` reported zero errors for all new modules. All 6 focused learning-track tests passed (lecturer draft creation/validation, catalogue/configuration topology, lifecycle and published-version immutability, discovery/enrollment safety, deterministic idempotent seeds, and legacy resolution). All 6 existing roadmap-generation regression tests passed. The existing 7-test assessment-journey suite completed with 5 passes and 2 score assertions failing because the accumulated local SQLite graph selected the alternate deterministic Python/Jac question set while those tests submit the first-set answers; no assessment behavior was changed, and local persisted data was intentionally not deleted.
- Feature 29 scope boundary: lecturer accounts, document upload/extraction, course-blueprint generation/review, enrollment, catalogue UI, and migration of assessment/roadmap/lesson/challenge/mastery records to required `track_id`/`track_version_id` fields remain deferred exactly as listed in the feature specification's non-goals.
- Feature 28 production deployment configuration is implemented without learner-facing or learning-logic changes. `jac.toml` now records the Jac Scale deployment extra, disables client debug mode and production API docs, suppresses health-check log noise, configures MongoDB plus Kubernetes secret injection for MongoDB/JWT/Gemini credentials, and defines a single-replica `graphlearn-production` deployment with CPU, memory, and health-probe settings.
- Feature 28 verification: `jac check main.jac` passed and `jac build main.jac` completed successfully, producing `.jac/client/dist/client.BOBYdtEP.js`. Existing focused journey suites passed for assessment (7 tests), roadmap generation (6 tests), lesson generation (6 tests), submission evaluation (10 tests), and mastery updates (13 tests); challenge generation and progression tests were passing until the two broader sequential commands reached their 15-minute caps because the accumulated local SQLite test graph is large. No test failure was reported before either timeout.
- Feature 28 deployment remains blocked at the final Jac Scale apply because Docker Desktop Kubernetes is disabled (`docker desktop kubernetes status` reports `State: disabled`) and there is no current `kubectl` context. Docker Desktop itself is running and Jac Scale 0.2.31 plus its `[deploy]` dependencies are installed. This Jac Scale release only permits `--dry-run` for the microservice target; the project is intentionally a single full-stack app, so forcing that target correctly rejects the missing microservice routes. Enable Docker Desktop Kubernetes, then run the supported full-stack apply with `jac start main.jac --scale --target kubernetes` (and configure an image registry before a production `--build`/push).
- Feature 27 observability has been implemented as lightweight internal infrastructure with no learner-facing or learning-logic changes. `lib/observability/` now provides structured JSON events, safe redaction, request/correlation identifiers, walker lifecycle timing, trace spans, low-cardinality in-memory metrics, slow thresholds, safe AI-call events, graph-query operation events, and transaction outcome event helpers. All 13 specified walkers emit start/completed/failed/slow lifecycle events through entry/exit abilities while preserving their existing required constructor fields and report/error shapes. AI generation/evaluation/recommendation calls emit safe lifecycle events without prompts, raw responses, tokens, or submitted code. Feature 26 graph helpers emit read-operation duration/affected-count events. `.env.example` documents safe production defaults and enable/disable switches.
- Feature 27 deterministic verification passes: `jac check main.jac`, all observability and instrumented graph modules, and 7 focused observability tests covering structured format, redaction, code metadata, success/failure lifecycle events, durations, slow detection, metrics, correlation propagation, safe AI success/failure, graph operations, and transaction commit/rollback events. Existing assessment generation/submission/evaluation, roadmap, lesson, challenge generation, submission evaluation, mastery, and initial skill-map regression suites completed successfully through 63 passing tests. The broader sequential regression command timed out after 15 minutes because the persisted local test graph has grown substantially; before timeout it exposed one pre-existing service-test expectation mismatch where `submit_challenge_solution` returns the implemented `completed` state after evaluation/mastery/progression while the test still expects `submitted`. No learner behavior was changed to satisfy that stale assertion.
- Current reassessment persistence issue has been fixed: `submit_assessment` no longer requires the learner to appear only as a direct `root` child after persistence recovery. It now resolves assessment ownership through the durable `Learner -[assigned]-> Assessment` topology first, falls back to the direct-root lookup for legacy/test graphs, and reuses those resolved nodes for attempt persistence instead of issuing a second brittle root-only query. This prevents a valid second assessment from failing with `Learner does not exist` after a completed roadmap while preserving learner/assessment ownership validation.
- Current challenge-editor enhancement has been implemented: the solution workspace now uses `@monaco-editor/react` as its primary controlled editor with Python language mode, a dark theme, indentation-aware options, automatic layout, and the existing draft/update callback. Jac challenges use Monaco's plaintext mode because Monaco has no built-in Jac language definition. The prior textarea remains an automatic fallback through a client error boundary and a ten-second Monaco initialization timeout. Monaco had not previously been blocked by a compatibility issue; it was intentionally omitted because Features 13–14 excluded it and Feature 15 permitted it only when already installed or planned.
- Current persisted lesson/challenge recovery issues have been fixed: the roadmap read service now derives a lesson's generated display state from the durable `GeneratedLesson` record when present instead of relying only on the roadmap outline node's cached status, so generated/completed lessons no longer regress to `pending` after restart. Challenge generation now canonicalizes harmless AI skill-id formatting differences back to the exact persisted roadmap target id, and a single-target lesson deterministically binds its criteria to that sole authoritative target; unrelated criteria remain rejected for multi-skill lessons. Scope remained limited to roadmap status projection and pre-persistence challenge criterion normalization; lesson generation, challenge content, evaluation, mastery, and progression rules were unchanged.
- Current persisted dashboard-recovery issue has been fixed: `load_dashboard` and its `get_dashboard` walker no longer assume string-backed mastery, lesson-progress, roadmap-lesson, progression-event, and roadmap-status enums always rehydrate as enum objects with a `.value` attribute. Dashboard comparisons and response projections now normalize those fields through `str(...)`, preserving their existing wire values for both live enum members and plain strings restored from SQLite/MongoDB. This removes the dashboard `500 EXECUTION_ERROR: 'str' object has no attribute 'value'` failure without changing aggregation, progression, mastery, roadmap, or activity behavior.
- Current persisted roadmap-recovery issue has been fixed: `load_roadmap` no longer assumes string-backed roadmap enums always rehydrate as enum objects with a `.value` attribute. Roadmap status and lesson generation status are now projected through `str(...)`, preserving the same API values for live enum members and plain strings restored from SQLite/MongoDB. This removes the roadmap `500 EXECUTION_ERROR: 'str' object has no attribute 'value'` failure without changing roadmap generation, graph topology, progression, or persistence behavior.
- Current persisted assessment-recovery issue has been fixed: `load_assessment_journey` no longer assumes string-backed assessment enums always rehydrate as enum objects with a `.value` attribute. Assessment status, question type, and difficulty are now projected through `str(...)`, which preserves the same wire values for live enum members and plain strings restored from SQLite/MongoDB. This removes the `500 EXECUTION_ERROR: 'str' object has no attribute 'value'` failure without changing assessment state, scoring, or persistence topology.
- Current assessment-submission issue has been fixed: `submit_and_evaluate_assessment` no longer trusts the client-provided question type when constructing persisted responses; it resolves each submitted question from the authenticated learner's persisted assessment and derives the authoritative response type from that node. This prevents stale compiled/restored client DTOs from producing `invalid_response_type` and a masked 500. Unknown question ids are rejected before mutation, structured walker validation messages are preserved, and the assessment UI now displays the safe server error instead of always replacing it with a generic message. Focused coverage verifies a deliberately stale client question type still completes and evaluates correctly. Scope remained limited to assessment response normalization and error visibility; scoring, evaluation rules, roadmap generation, mastery, and progression were unchanged.
- Current auth recovery issue has been fixed: after switching persistence backends, the client could send the stale `graphlearn.auth.token` from the former MongoDB user store to `/user/me` even though `jacLogin` had issued a fresh SQLite-backed Jac runtime token. Token lookup now prioritizes Jac runtime keys and uses the GraphLearn token only as a compatibility fallback, preventing valid-but-orphaned JWTs from producing `404 NOT_FOUND: User not found` immediately after a successful login. Scope was limited to token selection order; authentication rules, learner identity binding, graph ownership, and persistence behavior were not changed.
- Feature 26 has been respecified as “Graph Query and Persistence Helpers.” The previous ORM-style repository proposal is retired because Jac's typed graph traversal and automatic reachable-node persistence already provide the data-access model. The revised feature permits only demonstrated, repeated typed traversals, stable-key lookups, canonical ordering, structural ownership checks, and idempotency lookups; requires topology-revealing names such as `assessment_attempts(assessment)` and `roadmap_lessons(roadmap)`; keeps graph creation and domain mutation visible in walkers; and explicitly forbids generic CRUD/repository classes, entity-per-repository scaffolding, backend adapters, and invented save/commit/transaction wrappers. This was a specification-only change; no Jac application behavior was modified.
- Feature 25 durable graph persistence has been implemented: Jac's native graph store is now the documented and configured LMS source of truth (SQLite locally, MongoDB through `MONGODB_URI` when scaled); learner identity is bound to the server-derived authenticated root jid with auth `user_id` metadata and unambiguous legacy email-id adoption; login/registration/session recovery resolves the canonical learner through `/user/me`; learner-data services are authenticated/private so missing tokens can no longer write/read the shared guest root; learner schema additions use safe defaults; architecture/code standards and the persistence operations runbook are aligned; and a deterministic isolated cross-process test proves a graph record survives a complete Jac process exit/restart. `jac check` passed for the identity service, auth client/index, affected pages/services, persistence probe, and `main.jac`; `tests/persistence_restart_test.py -v` passed. The production build reached dependency installation but remains blocked by the existing Windows/Bun package-cache EPERM condition both sandboxed and after an escalated retry (which timed out). Production MongoDB restart/backup-restore acceptance remains a deployment verification gate because this environment cannot safely exercise the live production database.
- Current issue analysis/specification unit is complete: Feature 25 has been rewritten as a no-implementation durable graph persistence specification grounded in Jac's native persistence model. It documents the current restart failure classes, the mismatch between the repository's Jac graph implementation and the older PostgreSQL/Prisma proposal, authenticated root and stable learner identity requirements, graph reachability and schema-evolution rules, idempotency/atomicity expectations, a staged migration plan, and mandatory cross-process restart acceptance coverage. No application code, database configuration, graph schema, authentication behavior, or learner workflow was changed; implementation remains behind the explicit approval gate in the feature specification.
- Current issue-fix unit has been implemented and verified: dashboard and AI tutor assessment-state checks now follow the real persisted graph shape (`Assessment -> AssessmentAttempt -> AssessmentEvaluation`) instead of only scanning root-attached evaluation nodes, so learners with submitted/evaluated assessments and generated roadmaps no longer remain stuck in "Assessment in progress" / "Finish your assessment" states. Dashboard recent activity also includes linked assessment evaluations. The curated Python/Jac assessment initializer now rotates to a second deterministic template on repeat starts for the same learner, preserving the first-template test path while avoiding identical repeat assessments without requiring live AI calls. Scope remained limited to assessment state detection, dashboard/tutor recommendation correctness, and repeat assessment question variety: no mastery scoring changes, roadmap generation changes, lesson/challenge progression changes, tutor UI changes, or live provider dependency changes.
- Feature 24 ai-tutor-panel has been implemented and verified (thin `load_tutor_recommendation` service over the read-only `recommend_next_action` walker, dashboard AI Tutor panel, independent loading/error/empty/success states, priority badge, refresh action that re-calls only the recommendation service, safe CTA routing to existing assessment, roadmap, lesson, challenge, skill-map, or dashboard surfaces, endpoint registration, and focused service coverage). Scope remained limited to displaying one recommendation on the dashboard only: no frontend recommendation logic, tutor chat UI, learner state mutation, mastery update, lesson unlock, lesson generation, challenge generation, submission evaluation, dashboard aggregation changes, roadmap generation, certificates, notifications, or external-resource recommendations.
- Feature 23 recommend-next-action has been implemented and verified (read-only `recommend_next_action` walker, deterministic early recommendations for assessment, roadmap generation, roadmap start, generated lesson, challenge start, retry, weak-skill practice, completion, compact persisted context aggregation, structured byLLM tutor recommendation schemas/function, AI output normalization/validation, safe fallback on AI conversion/runtime failure, frontend DTO mirrors, endpoint registration, and focused MockLLM tests). Scope remained limited to returning a fresh tutor recommendation DTO only: no learner state mutation, mastery update, lesson unlock, lesson generation, challenge generation, submission evaluation, dashboard UI, tutor chat UI, notifications, certificates, or external-resource recommendations.
- Current issue-fix unit has been implemented and verified: challenge workspace starter code now renders from the actual `open_challenge` response by using a local fetched response object before updating reactive state, avoiding the stale `workspace_state` read that skipped editor initialization; skill-map loading no longer constructs `SkillMapLoadState` on the client, uses a local learner id before updating reactive state, and reads the local `load_skill_map` response before assigning state, preventing the `Function 'SkillMapLoadState' not found` 404 and stale "not ready" behavior. Scope remained limited to the two problems in `context/current-issues.md`: challenge editor response parsing/rendering and skill-map page/service wiring.
- Current issue-fix unit has been implemented and verified: challenge workspace now preserves generated starter code by ignoring empty/`undefined`/`null` local draft values and falling back through local draft, persisted draft, then generated starter code; `submit_challenge_solution` now persists the submission, runs `evaluate_submission`, applies `update_mastery` from the submission evaluation, and calls `unlock_next_lesson` when the evaluation passes; the workspace loads the latest evaluation and displays the evaluation score, feedback summary, and next step in the submit panel. Scope remained limited to wiring the already-implemented challenge/evaluation/mastery/progression path: no code execution, new challenge generation behavior, AI tutor, recommendations, certificates, or unrelated dashboard changes.
- Feature 22 dashboard-ui has been implemented and verified (thin `load_dashboard` service wrapper over the read-only `get_dashboard` walker, service-owned client DTO projection, authenticated `/dashboard` route, early-state dashboard panels, loading/error states, read-only progress/current-lesson/mastery/lesson/challenge/activity cards, login/register dashboard redirects, assessment-to-roadmap dashboard handoff, endpoint registration, and focused checks/tests). Scope remained limited to dashboard visualization only: no backend dashboard aggregation changes, recommendations, AI tutor, lesson generation, challenge generation, submission evaluation, mastery updates, lesson unlocking, roadmap generation, certificates, notifications, or graph mutation.
- Feature 21 get-dashboard has been implemented and verified (read-only `get_dashboard` walker, dashboard DTO objects, learner/roadmap resolution with optional language and roadmap id, safe early journey states, fallback roadmap progress summary, lesson/challenge/mastery/current-lesson/recent-activity aggregation, structured error handling, endpoint registration, frontend DTO mirrors, and focused tests). Scope remained limited to dashboard aggregation only: no dashboard UI, recommendations, AI tutor summary, mastery updates, lesson unlocking, roadmap generation, lesson generation, challenge generation, submission evaluation, certificates, or notifications.
- Feature 20 unlock-next-lesson has been implemented and verified (progression domain entities, persisted lesson/roadmap progress summaries, progression events, `unlock_next_lesson` walker orchestration, first-lesson initialization, completion validation from persisted generated lessons and passing submission evaluations, centralized prerequisite mastery threshold, clear locked reasons, roadmap completion handling, endpoint registration, frontend DTO mirrors, and focused tests). Scope remained limited to deterministic progression only: no lesson generation, challenge generation, submission evaluation, mastery updates, recommendations, dashboard UI, certificates, badges, notifications, or skill-map UI changes.
- Feature 19 skill-map-ui has been implemented and verified (thin `load_skill_map` service wrapper over the read-only `get_skill_map` walker, service-owned UI DTO projection, refresh-safe `/skill-map` and `/roadmap/:roadmapId/skill-map` routes, authenticated learner/language/roadmap context loading, roadmap header entry point, summary cards, read-only selectable skill node grid, dependency lists, selected-skill detail panel, mastery-level filters, loading/empty/error states, journey language recovery helper, endpoint registration, and focused checks/tests). Scope remained limited to visualization only: no mastery updates, lesson unlocking, recommendations, dashboard analytics, AI tutor actions, challenge regeneration, lesson generation, assessment generation, graph editing, or graph mutation.
- Feature 18 get-skill-map has been implemented and verified (read-only `get_skill_map` walker, learner/language/roadmap validation, active-roadmap resolution, roadmap skill collection from lesson targets and milestone requirements, SkillMastery overlays, prerequisite/teaches edge projection, target lesson ids, summary counts/average mastery, empty-map fallback, frontend DTO mirrors, endpoint registration, and focused tests). Scope remained limited to retrieving and formatting persisted graph data only: no mastery updates, lesson unlocks, roadmap generation, recommendations, dashboard UI, skill map visualization UI, AI tutor, challenge regeneration, certificates, or graph mutation.
- Feature 17 update-mastery has been implemented and verified (mastery domain entities, mastery evidence/result persistence, `update_mastery` walker orchestration, source-specific assessment/submission signal extraction, confidence-weighted deterministic score blending, centralized evidence weights, mastery level derivation, idempotent source reuse, graph links, frontend DTO mirrors, endpoint registration, and focused tests). Scope remained limited to updating learner skill mastery from persisted evaluation records only: no lesson unlocks, roadmap progress updates, dashboard aggregation, skill map UI, recommendations, challenge regeneration, lesson generation, AI tutor feedback, certificates, or frontend mastery UI.
- Feature 16 evaluate-submission has been implemented and verified (submission evaluation graph entities, structured byLLM evaluation schemas/context/function, `evaluate_submission` walker orchestration, idempotent existing-evaluation reuse, criterion-level scoring, evidence-only skill signals, structured feedback items, learner/challenge/submission/evaluation graph links, skill signal target links, centralized passing threshold, frontend DTO mirrors, endpoint registration, and MockLLM tests). Scope remained limited to static AI review of persisted submitted challenge code only: no unsafe code execution, mastery updates, lesson unlocking, roadmap progress updates, dashboard analytics, skill map updates, retry challenge generation, AI tutor chat, certificates, package installation, shell access, filesystem access, or network calls.
- Feature 15 challenge-workspace has been implemented and verified (lesson viewer challenge CTA, route-scoped challenge opener, `/roadmap/:roadmapId/lesson/:roadmapLessonId/challenge/:challengeId` workspace route, generated challenge display, editable starter-code workspace, local draft preservation, progressive hint reveal, focused `submit_challenge` persistence walker, thin challenge service functions, graph submission links, DTO mirrors, and focused tests). Scope remained limited to opening generated challenges and saving submitted code only: no code execution, terminal output, AI grading, `evaluate_submission`, mastery updates, unlock logic, dashboard analytics, skill map updates, AI tutor feedback, certificates, or challenge-generation behavior beyond start/open reuse.
- Feature 14 generate-challenge has been implemented and verified (generated challenge domain entities, structured challenge AI schemas/context/function, `generate_challenge` walker orchestration, persisted instructions/outcomes/constraints/hints/evaluation criteria, challenge-to-lesson/roadmap/learner/skill graph links, idempotent existing challenge reuse, frontend DTO mirrors, endpoint registration, and MockLLM tests). Scope remained limited to challenge generation only: no challenge workspace UI, Monaco/editor integration, code execution, submission storage, AI grading, mastery updates, unlock logic, dashboard analytics, skill map visualization, AI tutor recommendations, certificates, or authentication changes.
- Feature 13 lesson-viewer has been implemented and verified (thin `open_lesson` service, `/roadmap/:roadmapId/lesson/:roadmapLessonId` route, generated lesson load-or-generate behavior, section navigation, lesson metadata/header, static code examples with copy-to-clipboard, mini-exercise local check state, local read completion, previous/next lesson navigation, roadmap lesson "Open lesson" CTAs, loading/error states, and faux endpoint registration). Scope remained limited to generated lesson viewing only: no coding challenge generation, Monaco/editor workspace, code execution, submission evaluation, mastery updates, unlock logic, dashboard analytics, skill map updates, AI tutor chat, certificates, or authentication changes.
- Feature 12 generate-lesson has been implemented and verified (generated lesson content nodes, structured lesson AI schemas/context/function, `generate_lesson` walker orchestration, persisted section/example/mini-exercise/takeaway nodes, idempotent reuse, roadmap lesson generation status update, generated lesson view DTOs, frontend TypeScript DTOs, and MockLLM tests). Scope remained limited to on-demand lesson generation only: no lesson viewer UI, coding workspace, challenge generation, submission evaluation, mastery updates, unlock logic, dashboard analytics, skill map visualization, AI tutor recommendations, certificates, or authentication changes.
- Current issue-fix unit has been implemented and verified: `main.jac` now explicitly imports `generate_roadmap_for_journey` and `load_roadmap` so both RPC functions register on cold server startup; `generate_roadmap` now reports a typed `Roadmap` node consistently for new and idempotent paths; `generate_roadmap_for_journey` normalizes roadmap ids from typed or legacy dict reports; assessment short-answer and learner-id inputs now set dark-theme text, caret, and placeholder token classes. Scope remained limited to endpoint registration/reliability, roadmap handoff report shape, and assessment input contrast.
- Current issue-fix unit has been implemented and verified: frontend auth flow now gates assessment start, learner identity is passed through assessment/evaluation/roadmap service calls, journey ids are persisted in localStorage, results include an explicit "Generate My Roadmap" CTA, roadmap generation reuses existing active roadmaps, and successful generation navigates to `/roadmap/:roadmapId`. Scope remained limited to auth and assessment-to-roadmap integration: no lesson generation, challenge generation, mastery updates, unlock logic, dashboard analytics, skill map, AI tutor, certificates, password reset, email verification, social login, or admin/role features.
- Feature 11 roadmap-ui has been implemented and verified (read-only roadmap read service, `/roadmap/:roadmapId` route, roadmap page shell, header, summary cards, ordered timeline, lesson outline rows, milestone rows, loading skeletons, empty/error states, DTO aliases, and service projection tests). Scope remains limited to roadmap visualization only: no lesson generation, lesson workspace, coding challenges, unlock logic, mastery updates, dashboard analytics, skill map visualization, AI tutor, certificates, or authentication.
- Feature 10 generate-roadmap has been implemented and verified (roadmap domain entities, structured roadmap AI schemas/function, `generate_roadmap` walker orchestration, active-roadmap idempotency, graph persistence/retrieval, skill target/requirement edges, frontend DTOs, and MockLLM tests). Scope remains limited to roadmap outline generation only: no roadmap UI, dashboard UI, lesson content generation, challenge generation, mastery updates, lesson unlocking, progress tracking, tutoring, recommendations, certificates, or authentication.
- Feature 09 landingpage-ui is now in progress. Scope is limited to the Phase 1 assessment journey: landing page, Python/Jac track selection, assessment start, assessment UI, submission, evaluation, results display, refresh recovery, and failure states. Do not implement mastery, learner progression, adaptive assessments, roadmaps, challenges, tutoring, recommendations, achievements, authentication, or additional languages.
- Feature 08 evaluate-assessment has been implemented (evaluation domain records, scoring service, walker orchestration, idempotent persistence, skill evidence signals, and focused tests). Scope remains limited to deterministic evaluation only: no mastery, learner model, progression, recommendation, learning plan, unlocking, or assessment generation updates.
- Feature 07 submit-assessment persistence has been implemented (walker, domain nodes, validation, graph links, retrieval helpers, and mock tests). Scope remains limited to persistence only: no scoring, grading, mastery calculation, roadmap generation, recommendations, or learner feedback.
- Feature 06 assessment-taking UI is implemented: dedicated `features/assessment/` module, UI-safe assessment views, centralized response state, validation, progress tracking, multiple-choice and short-answer rendering, submission success/failure states, and save-only answer persistence.
 Confirmed the assessment domain node foundation already exists in `nodes/assessment.jac` with `Assessment`, `AssessmentQuestion`, `AssessmentOption`, and assessment-specific enums.
 Confirmed the `initialize_assessment` walker already persists assessments, questions, options, and learner assignment links using structured byLLM outputs only.
 Verified the assessment feature slice with `jac check nodes/assessment.jac`, `jac check walkers/initialize_assessment.jac`, and `jac test lib/ai/tests/assessment_test.jac -v`.
- Feature 05 is implemented and verified. Prepare for the next isolated feature spec.


- Read `AGENTS.md`, `context/ui-context.md`, and supporting architecture/workflow context.
- Added shadcn/ui configuration scaffolding, `components/ui/`, and shared `lib/utils.cl.jac`.
- Installed the required generated shadcn/ui primitives: Button, Card, Dialog, Input, Tabs, Textarea, and ScrollArea.
- Removed generated guestbook UI from the active app entry.
- Created `walkers/`, `nodes/`, `edges/`, `lib/`, `components/`, and `features/` structure.
- Updated the static app shell status from setup/design-system-next to design-system/domain-models-next.
- Read `context/feature-specs/02-domain-models.md` and implemented the isolated domain model foundation.
- Added `byllm` to project dependencies and confirmed byLLM 0.6.18 is installed and registered as a Jac plugin.
- Added reusable AI schema exports through `lib/ai/schemas.jac`.
- Added centralized byLLM utility functions and standard byLLM error handling policy in `lib/ai/utilities.jac`.
- Added MockLLM structured-output tests in `lib/ai/tests/ai_test.jac`; tests run without API keys.
- Verified no OpenAI, Anthropic, Gemini, Google, provider wrapper, API key, or prompt-wrapper patterns were introduced in LMS source code.
- Read `context/feature-specs/05-initialize-assessment.md`; `context/ai-context.md` and `context/domain-context.md` were requested by the spec but are not present in the repo.
- Promoted assessment storage from placeholder question dictionaries to graph nodes: `Assessment`, `AssessmentQuestion`, and `AssessmentOption`, with typed question and option link edges.
- Added structured assessment AI schemas and reusable byLLM functions for assessment title, description, questions, answer options, and composed initial assessment generation.
- Added the `initialize_assessment` walker to generate, validate, persist, connect, and report an initial assessment without scoring, grading, mastery calculation, or roadmap generation.
- Added MockLLM tests for structured assessment generation, assessment entity creation, question/option creation, graph persistence, and retrieval through typed graph links.
- Registered the assessment walker through `main.jac` and exported assessment domain types through `lib/domain/index.jac`.
- Updated frontend mirror assessment types in `lib/types/index.ts` to match the persisted assessment, question, and option entities.

## In Progress

- No feature is currently in progress; Feature 34 and its reported post-migration regressions are implemented and verified.

## Next Up

- Proceed to the next isolated feature specification after Feature 34 verification.
- Proceed to the next isolated feature specification after Feature 30 verification.
- Choose the next isolated feature spec after Feature 22 dashboard-ui, likely recommendation UI, AI tutor summary, roadmap completion experience, certificates, or notifications when a spec asks for it.
- Choose the next isolated feature spec after Feature 21 get-dashboard, likely dashboard UI, recommendation UI, AI tutor summary, or completion experience when a spec asks for it.
- Choose the next isolated feature spec after Feature 20 unlock-next-lesson, likely dashboard summary, roadmap progress UI integration, recommendation UI, or completion experience when a spec asks for it.
- Choose the next isolated feature spec after Feature 19 skill-map-ui, likely dashboard summary, unlock/progression, submission feedback, or recommendation UI when a spec asks for it.
- Choose the next isolated feature spec after Feature 18 get-skill-map, likely a skill map UI, dashboard summary, unlock/progression, or recommendation feature when a spec asks for it.
- Choose the next isolated feature spec after Feature 16 evaluate-submission, likely a submission feedback UI, mastery update, or unlock/progression feature when a spec asks for it.
- Choose the next isolated feature spec after Feature 12 generate-lesson, likely a lesson viewer/workspace feature when a spec asks for it.
- Choose the next isolated feature spec after Feature 10 generate-roadmap, or wire Feature 10 into a future roadmap UI/dashboard feature when a spec asks for it.
- Choose the next isolated feature spec after resolving or accepting the existing Windows client-build permission limitation.

## Open Questions

- `13-lesson-viewer.md` asks to read `context/domain-context.md`, `context/roadmap-context.md`, and `context/lesson-context.md`, but those files are not present; implementation is proceeding from `AGENTS.md`, project overview, architecture, UI context, code standards, AI workflow rules, progress tracker, and the implemented roadmap/lesson modules.
- `09-landingpage-ui.md` asks to read `context/domain-context.md` and `context/assessment-context.md`, but those files are not present; implementation is proceeding from `AGENTS.md`, project overview, architecture, UI context, code standards, AI workflow rules, progress tracker, and the implemented assessment modules.
- `05-initialize-assessment.md` asks to read `context/ai-context.md` and `context/domain-context.md`, but those files are not present; implementation is proceeding from `AGENTS.md`, project overview, architecture, code standards, AI workflow rules, and the Feature 05 spec.
- The system `jac` shim at `C:\Python\Python39\Scripts\jac.exe` still returns non-zero without diagnostics; use the project venv `.\.jac\venv\Scripts\jac.exe` with UTF-8 output enabled.
- `jac install` can time out during npm install; direct Bun install from `.jac/client/configs` completed after package extraction/linking.
- Direct Vite production build is currently blocked by generated-client resolution of `@jac/runtime`; `jac build` is also blocked by Windows access denial on `.jac/client/node_modules/@esbuild/win32-x64/esbuild.exe`, even with escalation.

## Architecture Decisions

- Use Jac built-in auth endpoints (`/user/register`, `/user/login`) through the frontend auth boundary so array-wrapped and plain auth envelopes are normalized consistently; use Jac runtime helpers for token installation (`jacSetToken`), logout, and logged-in checks, with localStorage only for current-stage recovery of normalized learner/session metadata and journey ids.
- Assessment journey service calls must receive an explicit learner id from the authenticated frontend state; the prior hardcoded phase-1 learner id remains only as a default for older tests/local calls.
- Feature 10 idempotency strategy is "return existing active roadmap" for the same learner, language, assessment, attempt, and evaluation instead of creating duplicate active roadmaps.
- Keep Feature 05 scoped to assessment initialization only: no scoring, grading, mastery calculation, answer evaluation, or roadmap generation.
- Keep Feature 06 scoped to rendering generated assessments and persisting learner answers only; submission does not invoke learner-outcome workflows.
- Use `services/assessment.sv.jac` as a thin boundary that projects assessment graph entities into UI-safe views and stores answer payloads as `AssessmentSubmission` nodes.
- Keep AI output schemas as plain `obj` values and copy validated structured fields into graph `node` entities for persistence.
- Use Jac Client + Tailwind token classes for the app shell.
- Treat `components/ui/` as managed shadcn/ui space and avoid hand-writing primitives there.

## Session Notes

- Feature 22: Added `services/dashboard.sv.jac` as a thin read boundary over `get_dashboard`, including UI-facing dashboard load state and projection DTOs for learner, roadmap, progress, mastery, lesson, challenge, current lesson, and recent activity data.
- Feature 22: Added `features/dashboard/pages/DashboardPage.cl.jac` and `features/dashboard/components/DashboardComponents.cl.jac` for the authenticated `/dashboard` route, loading skeletons, error handling, early journey states (`no_assessment`, `assessment_started`, `roadmap_missing`, `roadmap_ready`, `completed`), and the full read-only dashboard for ready/in-progress/completed learner states.
- Feature 22: Updated `components/AppShell.cl.jac` to guard `/dashboard`, updated login and registration success navigation to `/dashboard`, updated assessment roadmap generation handoff to return to `/dashboard`, and registered `load_dashboard` through `main.jac`.
- Feature 22 verification passed: `jac check services/dashboard.sv.jac`, `jac check features/dashboard/pages/DashboardPage.cl.jac`, `jac check features/dashboard/components/DashboardComponents.cl.jac`, `jac check components/AppShell.cl.jac`, `jac check features/auth/pages/LoginPage.cl.jac`, `jac check features/auth/pages/RegisterPage.cl.jac`, `jac check features/assessment/pages/AssessmentJourneyPage.cl.jac`, `jac check lib/dashboard/tests/dashboard_service_test.jac`, `jac check main.jac`, `jac test lib/dashboard/tests/dashboard_service_test.jac -v`, `jac test lib/dashboard/tests/get_dashboard_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/function/load_dashboard` and `/walker/get_dashboard`.
- Feature 22 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated runs passed. Standalone client Jac checks still emit existing module-resolution/type warnings for `react-router-dom`, `lucide-react`, and shadcn/runtime JSX symbols, but exit successfully. Faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed. A hidden `jac start --dev main.jac` attempt launched but did not bind a local port or emit logs, so the non-serving process was stopped; live browser QA remains unverified.
- Current issue-fix: Updated `features/challenge/pages/ChallengeWorkspacePage.cl.jac` so `load_workspace` stores the `open_challenge` result in a local `fetched_state`, then uses that local value for starter-code/draft/submission/evaluation initialization before rendering. This fixes the stale reactive-state read that prevented `challenge.starter_code` from reaching the editor even when present in the response payload.
- Current issue-fix: Updated `features/skill_map/pages/SkillMapPage.cl.jac` so `fetch_skill_map` handles missing auth with local UI state instead of constructing the server DTO `SkillMapLoadState` on the client, uses a local `learner_id_text` for validation and the service call, and uses the local `fetched_state` for first-skill selection before assigning `load_state`.
- Current issue-fix verification passed: `jac check features/challenge/pages/ChallengeWorkspacePage.cl.jac`, `jac check features/skill_map/pages/SkillMapPage.cl.jac`, `jac check services/challenge.sv.jac`, `jac check services/skill_map.sv.jac`, `jac check main.jac`, and `jac test lib/skill_map/tests/get_skill_map_test.jac`. `jac build main.jac` still timed out in the known Windows client-build path, but it regenerated client files far enough to verify compiled output uses `fetched_state` for challenge loading, `learner_id_text` for skill-map loading, and no longer emits a client call to `SkillMapLoadState`.
- Current issue-fix notes: `jac test lib/challenge/tests/submit_challenge_test.jac` reaches the real AI-backed evaluation path introduced by the prior challenge workflow wiring and fails in this sandbox with `WinError 10013` network denial; this is not a failure of the current starter-code or skill-map fixes. Existing standalone client checks continue to emit dependency/type-resolution warnings for React router/lucide/shadcn symbols but exit successfully.
- Current challenge workflow issue-fix: Updated `services/challenge.sv.jac` so `open_challenge` also returns the latest persisted submission evaluation, and `submit_challenge_solution` now chains persisted submission -> static submission evaluation -> mastery update -> progression unlock for passing evaluations while preserving partial-success messages for evaluation/mastery/progression failures.
- Current challenge workflow issue-fix: Updated `features/challenge/pages/ChallengeWorkspacePage.cl.jac` to ignore unusable local draft values (`""`, `undefined`, `null`) before falling back to persisted drafts and generated starter code, clear bad drafts, load existing evaluation feedback, and display fresh evaluation feedback after submission.
- Current challenge workflow issue-fix: Updated `features/challenge/components/ChallengeWorkspaceComponents.cl.jac` to describe the static evaluation flow and render evaluation score, pass/fail tone, feedback summary, and suggested next step in the submit panel.
- Current challenge workflow issue-fix verification passed: `jac check services/challenge.sv.jac`, `jac check features/challenge/pages/ChallengeWorkspacePage.cl.jac`, `jac check features/challenge/components/ChallengeWorkspaceComponents.cl.jac`, `jac check main.jac`, `jac test lib/challenge/tests/evaluate_submission_test.jac`, `jac test lib/mastery/tests/update_mastery_test.jac`, and `jac test lib/progression/tests/unlock_next_lesson_test.jac`. Standalone client checks still emit existing dependency/type-resolution warnings for React router/lucide/shadcn symbols but exit successfully; MockLLM tests emitted the existing LiteLLM remote cost-map network fallback warning and completed without live provider keys.
- Feature 21: Added `walkers/get_dashboard.jac` as a pure read model with `DashboardView`, learner/roadmap/progress/mastery/lesson/challenge/current-lesson/recent-activity DTOs, optional language/roadmap resolution, active-roadmap fallback, safe no-assessment/assessment-started/roadmap-missing/roadmap-ready/in-progress/completed states, and structured errors for invalid learner/language/roadmap/progress references.
- Feature 21: Added TypeScript dashboard DTO mirrors in `lib/types/index.ts`, registered `/walker/get_dashboard` through `main.jac`, and added focused tests in `lib/dashboard/tests/get_dashboard_test.jac` covering early states, roadmap fallback, progression aggregation, generated lessons/challenges, submissions/evaluations, mastery strongest/weakest skills, current lesson resolution, recent activity ordering, completed state, and invalid references.
- Feature 21 verification passed: `jac check walkers/get_dashboard.jac`, `jac check lib/dashboard/tests/get_dashboard_test.jac`, `jac check main.jac`, `jac test lib/dashboard/tests/get_dashboard_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/get_dashboard`.
- Feature 21 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`. Faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed successfully.
- Feature 20: Added `nodes/progression.jac` with `LessonProgress`, `RoadmapProgress`, `ProgressionEvent`, and progression enums; added progression graph edges (`progresses`, `tracks`, `unlocked`, `belongs_to`) while reusing existing roadmap/challenge/evaluation/mastery entities as sources of truth.
- Feature 20: Implemented `walkers/unlock_next_lesson.jac` with learner/roadmap ownership validation, ordered lesson loading, malformed-order rejection, idempotent first-lesson initialization, persisted completion validation from `GeneratedLesson` and passed `SubmissionEvaluation`, mastery-aware next-lesson unlock with a centralized minimum prerequisite score of 40, locked reasons, roadmap completion, and typed progression result DTOs.
- Feature 20: Added frontend DTO mirrors in `lib/types/index.ts`, exported progression domain types, registered `/walker/unlock_next_lesson`, and documented the progression storage model in `context/architecture-context.md`.
- Feature 20 verification passed: `jac check walkers/unlock_next_lesson.jac`, `jac check nodes/progression.jac`, `jac check lib/progression/tests/unlock_next_lesson_test.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `jac test lib/progression/tests/unlock_next_lesson_test.jac -v`, TypeScript `tsc --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/unlock_next_lesson`.
- Feature 20 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. The focused progression test suite passed all 12 tests but took about four minutes because existing persisted local graph test data makes root scans slow. Faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed successfully.
- Feature 19: Added `services/skill_map.sv.jac` as a thin read-only UI service that resolves language from explicit state or roadmap context, validates learner/roadmap ownership before delegation where needed, spawns `get_skill_map`, and projects walker reports into service-owned DTOs for client imports.
- Feature 19: Added `features/skill_map/pages/SkillMapPage.cl.jac` and `features/skill_map/components/SkillMapComponents.cl.jac` with `/skill-map` and `/roadmap/:roadmapId/skill-map` route support, auth/session recovery, summary cards, mastery-level filters, a read-only selectable skill graph/list, accessible dependency lists, selected-skill details, skeleton loading, empty state, and clear error actions.
- Feature 19: Added a roadmap header "Skill map" entry point and `read_journey_language()` helper for refresh-safe language recovery without calling assessment or roadmap generation flows from the skill-map page.
- Feature 19 verification passed: `jac check services/skill_map.sv.jac`, `jac check lib/journey/journey_state.cl.jac`, `jac check features/skill_map/components/SkillMapComponents.cl.jac`, `jac check features/skill_map/pages/SkillMapPage.cl.jac`, `jac check features/roadmap/components/RoadmapComponents.cl.jac`, `jac check components/AppShell.cl.jac`, `jac check main.jac`, `jac check lib/skill_map/tests/get_skill_map_test.jac`, `jac test lib/skill_map/tests/get_skill_map_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/function/load_skill_map` and `/walker/get_skill_map`.
- Feature 19 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. Standalone client Jac checks still emit existing module-resolution/type warnings for `react-router-dom`, `lucide-react`, and shadcn/runtime JSX symbols but exit successfully. Faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed successfully without live AI providers.
- Feature 18: Implemented `walkers/get_skill_map.jac` as a read-only graph view with structured `SkillMapView`, `SkillMapSummary`, `SkillMapSkill`, and `SkillMapEdge` report DTOs.
- Feature 18: The walker validates learner/language/roadmap ownership, resolves the active roadmap when no roadmap id is supplied, returns a safe empty map when no active roadmap exists, and reads roadmap lesson target skills, milestone required skills, SkillMastery records, target lesson ids, and prerequisite/teaches relationships without mutating graph state.
- Feature 18: Added frontend DTO mirrors for `SkillMapView`, `SkillMapSummary`, `SkillMapSkill`, and `SkillMapEdge`.
- Feature 18 verification passed: `jac check walkers/get_skill_map.jac`, `jac check main.jac`, `jac check lib/skill_map/tests/get_skill_map_test.jac`, `jac test lib/skill_map/tests/get_skill_map_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/get_skill_map`.
- Feature 18 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. Faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed successfully without live AI providers.
- Feature 17: Added `SkillMastery`, `MasteryEvidence`, `MasteryUpdateResult`, `UpdatedSkillMasteryRecord`, `MasteryLevel`, and `MasterySourceType` while keeping the legacy `Mastery` node intact for compatibility.
- Feature 17: Added the `evidenced_by` graph edge and used existing `mastered`, `signals`, and `targets` relationships for learner-to-mastery, evaluation evidence, and skill links without introducing unlock or progress edges.
- Feature 17: Implemented `walkers/update_mastery.jac` with learner/source validation, assessment/submission evaluation lookup, skill signal extraction, invalid score/confidence rejection, missing skill rejection, evidence persistence, deterministic confidence-weighted blending, score clamping, level derivation, and idempotent `MasteryUpdateResult` reuse.
- Feature 17: Added frontend DTO mirrors for `MasteryUpdateSummary`, `UpdatedSkillMastery`, `SkillMasteryView`, and `MasteryEvidenceView`.
- Feature 17 verification passed: `jac check nodes/mastery.jac`, `jac check walkers/update_mastery.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `jac check lib/mastery/tests/update_mastery_test.jac`, `jac test lib/mastery/tests/update_mastery_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/update_mastery`.
- Feature 17 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. The focused test run emitted existing local SQLite schema-drift info from persisted test data, and faux startup emitted the existing LiteLLM remote cost-map network fallback warning; both completed successfully without live AI providers.
- Feature 16: Added `SubmissionEvaluation`, `CriterionResult`, `SubmissionSkillSignal`, `FeedbackItem`, and `FeedbackType` domain entities, plus `evaluated` and `signals` graph edges for separate persisted submission review evidence.
- Feature 16: Added structured submission-evaluation byLLM objects and `evaluate_code_submission` in `lib/ai/utilities.jac`; all AI-visible fields use `sem` descriptions and the evaluation context is built from persisted challenge, lesson, criteria, target skill, and submission data.
- Feature 16: Implemented `walkers/evaluate_submission.jac` with learner/challenge/submission ownership validation, submitted-status validation, unsupported-language rejection, missing-context rejection, existing-evaluation reuse, AI output validation, score normalization, centralized pass threshold at 70, and graph persistence for evaluation children and relationships.
- Feature 16: Added frontend DTO mirrors for `SubmissionEvaluationView`, `CriterionResultView`, `SubmissionSkillSignalView`, and `FeedbackItemView`.
- Feature 16 verification passed: `jac check walkers/evaluate_submission.jac`, `jac check nodes/challenge.jac`, `jac check lib/ai/utilities.jac`, `jac check lib/ai/schemas.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `jac check lib/challenge/tests/evaluate_submission_test.jac`, `jac test lib/challenge/tests/evaluate_submission_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/evaluate_submission`.
- Feature 16 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. MockLLM and faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed without API keys.
- Feature 15: Added `ChallengeSubmission` and `ChallengeSubmissionStatus`, plus `attempted` and `submitted_for` graph edges for learner/challenge/submission persistence without any evaluation or execution behavior.
- Feature 15: Implemented `walkers/submit_challenge.jac` with learner, roadmap, roadmap lesson, challenge ownership, language, and non-empty-code validation; submissions are immutable submitted records linked from learner to submission and generated challenge to submission.
- Feature 15: Added `services/challenge.sv.jac` with `open_challenge`, `start_challenge`, and `submit_challenge_solution` as thin route/service boundaries. `start_challenge` reuses an existing generated challenge or delegates to the existing `generate_challenge` walker; `open_challenge` validates the route context before returning workspace state.
- Feature 15: Added the challenge workspace client page and presentation components, wired `/roadmap/:roadmapId/lesson/:roadmapLessonId/challenge/:challengeId`, and added the lesson viewer "Start coding challenge" CTA. Draft code is preserved locally in browser storage; submit persists final code only.
- Feature 15 verification passed: `jac check nodes/challenge.jac`, `jac check walkers/submit_challenge.jac`, `jac check walkers/generate_challenge.jac`, `jac check services/challenge.sv.jac`, `jac check lib/challenge/tests/submit_challenge_test.jac`, `jac check lib/domain/index.jac`, `jac check features/challenge/components/ChallengeWorkspaceComponents.cl.jac`, `jac check features/challenge/pages/ChallengeWorkspacePage.cl.jac`, `jac check features/lesson/components/LessonComponents.cl.jac`, `jac check features/lesson/pages/LessonPage.cl.jac`, `jac check components/AppShell.cl.jac`, `jac check main.jac`, `jac test lib/challenge/tests/submit_challenge_test.jac -v`, `jac test lib/challenge/tests/generate_challenge_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/function/open_challenge`, `/function/start_challenge`, `/function/submit_challenge_solution`, and `/walker/submit_challenge`.
- Feature 15 notes: TypeScript verification required escalation because the sandbox hit Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. Standalone client Jac checks still emit existing module-resolution/type warnings for `react-router-dom`, `lucide-react`, and shadcn/runtime JSX symbols, but exit successfully. MockLLM and faux startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed.
- Feature 14: Extended `nodes/challenge.jac` with `GeneratedChallenge`, `ChallengeInstruction`, `ChallengeExpectedOutcome`, `ChallengeConstraint`, `ChallengeHint`, and `ChallengeEvaluationCriterion`; added a minimal `evaluates` edge while reusing `assigned`, `contains`, and `targets`.
- Feature 14: Added structured challenge generation objects and `generate_coding_challenge` in `lib/ai/utilities.jac`, exported them from `lib/ai/schemas.jac`, and kept all AI-visible fields covered by `sem` descriptions.
- Feature 14: Implemented `walkers/generate_challenge.jac` with learner/roadmap/roadmap lesson/generated lesson validation, language validation, assessment skill context loading, existing challenge reuse, structured AI output validation, persistence, graph relationship creation, and typed `GeneratedChallengeView` projection.
- Feature 14: Added frontend DTO mirrors in `lib/types/index.ts`, domain barrel exports, and `main.jac` walker registration.
- Feature 14 verification passed: `jac check nodes/challenge.jac`, `jac check lib/ai/utilities.jac`, `jac check lib/ai/schemas.jac`, `jac check walkers/generate_challenge.jac`, `jac check lib/challenge/tests/generate_challenge_test.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `jac test lib/challenge/tests/generate_challenge_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client` with UTF-8 output enabled. Faux docs listed `/walker/generate_challenge`.
- Feature 14 notes: TypeScript verification required escalation because the sandbox hit Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`. Faux startup needed UTF-8 output environment variables to avoid the known Windows console encoding failure while printing arrow symbols. MockLLM tests emitted the existing LiteLLM remote cost-map network fallback warning and local SQLite schema-drift info, then completed successfully.
- Feature 13: Implemented `services/lesson.sv.jac` with `open_lesson`, existing-generated-lesson reuse, on-demand `generate_lesson` spawning, route/learner/roadmap validation, and previous/next lesson link projection.
- Feature 13: Added `features/lesson/pages/LessonPage.cl.jac`, `features/lesson/components/LessonComponents.cl.jac`, and client lesson view DTO mirrors; wired `/roadmap/:roadmapId/lesson/:roadmapLessonId` in `components/AppShell.cl.jac`; added roadmap "Open lesson" CTAs without triggering generation on the roadmap page.
- Feature 13: Extended `GeneratedLessonView` with `roadmap_id` and `updated_at` in Jac and TypeScript so the viewer has stable generated lesson metadata.
- Feature 13 verification passed: `jac check services/lesson.sv.jac`, `jac check walkers/generate_lesson.jac`, `jac check features/lesson/types/lesson_view_models.cl.jac`, `jac check features/lesson/components/LessonComponents.cl.jac`, `jac check features/lesson/pages/LessonPage.cl.jac`, `jac check features/roadmap/components/RoadmapComponents.cl.jac`, `jac check components/AppShell.cl.jac`, `jac check main.jac`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/function/open_lesson` and `/walker/generate_lesson`.
- Feature 13 notes: TypeScript verification required escalation because the sandbox hit Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. Standalone client Jac checks still emit existing module-resolution/type warnings for `react-router-dom`, `lucide-react`, and shadcn/runtime JSX symbols, but exit successfully. Faux server startup emitted the existing LiteLLM remote cost-map network fallback warning, then completed.
- Feature 12: Implemented on-demand lesson generation with `GeneratedLesson`, `LessonSection`, `LessonExample`, `LessonMiniExercise`, `LessonTakeaway`, `LessonGenerationStatus`, structured lesson AI outputs, `LessonContentContext`, `generate_lesson_content`, and the `generate_lesson` walker.
- Feature 12 verification passed: `jac check nodes/lesson.jac`, `jac check lib/ai/utilities.jac`, `jac check lib/ai/schemas.jac`, `jac check walkers/generate_lesson.jac`, `jac check lib/lesson/tests/generate_lesson_test.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `jac test lib/lesson/tests/generate_lesson_test.jac -v`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/generate_lesson`.
- Feature 24: Added `services/tutor.sv.jac` with service-owned `TutorRecommendationView` and `TutorRecommendationLoadState` DTOs. `load_tutor_recommendation` validates the learner id, delegates to the existing `recommend_next_action` walker, projects typed reports into a dashboard-safe state object, and surfaces walker/service failures without mutating graph state.
- Feature 24: Added `features/tutor/components/AiTutorPanel.cl.jac` with dashboard-safe loading, error, empty, and recommendation states; priority styling; refresh; and CTA route mapping for assessment, roadmap, lesson, challenge, skill-map, and dashboard fallbacks. The component contains no recommendation ranking or learner-state mutation logic.
- Feature 24: Integrated `AiTutorPanel` into `features/dashboard/pages/DashboardPage.cl.jac` for both early-state and full dashboard layouts, deriving learner, roadmap, and language context from the existing dashboard load result. Registered `/function/load_tutor_recommendation` through `main.jac`.
- Feature 24 verification passed: `jac check services/tutor.sv.jac`, `jac check features/tutor/components/AiTutorPanel.cl.jac`, `jac check features/dashboard/pages/DashboardPage.cl.jac`, `jac check lib/tutor/tests/tutor_service_test.jac`, `jac test lib/tutor/tests/tutor_service_test.jac -v`, `jac test lib/tutor/tests/recommend_next_action_test.jac -v`, `jac check main.jac`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/function/load_tutor_recommendation` and `/walker/recommend_next_action`.
- Feature 24 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. Standalone client Jac checks still emit existing module-resolution/type warnings for `react-router-dom`, `lucide-react`, and shadcn/runtime JSX symbols, but exit successfully. MockLLM/faux startup emitted the existing LiteLLM remote cost-map network fallback warning and completed without API keys. A scoped scan over `services/tutor.sv.jac`, `features/tutor`, and the dashboard integration found no graph mutation, mastery, unlock, generation, or evaluation calls.
- Feature 23: Added structured tutor recommendation AI schemas in `lib/ai/utilities.jac` (`TutorRecommendationType`, `TutorRecommendationPriority`, `TutorRecommendationReason`, `TutorRecommendedResource`, `TutorRecommendationContext`, `TutorRecommendationResult`) and exported them through `lib/ai/schemas.jac`; added `generate_next_action_recommendation` as the reusable byLLM function with LMS-only scope instructions.
- Feature 23: Implemented `walkers/recommend_next_action.jac` as a read-only graph aggregation walker over learner, assessment, roadmap, progression, generated lesson/challenge, submissions/evaluations, mastery, mastery evidence, and recent progression events. It applies deterministic pre-checks before byLLM, clamps confidence, normalizes priorities/types/related ids, rejects invalid references and malformed progress/mastery data, validates converted AI display fields, and falls back safely if AI conversion or runtime shape handling fails.
- Feature 23: Added frontend DTO mirrors for `TutorRecommendationView`, `TutorRecommendationType`, and `TutorRecommendationPriority` in `lib/types/index.ts`, and registered `/walker/recommend_next_action` through `main.jac`.
- Feature 23 verification passed: `jac check lib/ai/utilities.jac`, `jac check walkers/recommend_next_action.jac`, `jac check lib/tutor/tests/recommend_next_action_test.jac`, `jac test lib/tutor/tests/recommend_next_action_test.jac -v`, `jac check lib/ai/schemas.jac`, `jac check main.jac`, `.jac/client/node_modules/.bin/tsc.exe --noEmit`, and `jac start main.jac --faux --no_client`. Faux docs listed `/walker/recommend_next_action`.
- Feature 23 notes: TypeScript verification required escalation because the sandbox hit the existing Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. MockLLM/faux startup emitted the existing LiteLLM remote cost-map network fallback warning and completed without API keys. `jac check walkers/recommend_next_action.jac` passes with non-blocking style warnings for helper parameter count in compact DTO/context builders.
- Feature 12 TypeScript verification required escalation because the sandbox hit Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`; the escalated run passed. MockLLM test runs emitted the existing LiteLLM remote cost-map network fallback warning and SQLite schema-drift info from persisted local test data, then completed successfully.
- Current issue-fix endpoint/input verification passed: `jac check main.jac`, `jac check walkers/generate_roadmap.jac`, `jac check services/assessment_journey.sv.jac`, `jac check services/roadmap.sv.jac`, `jac check lib/roadmap/tests/generate_roadmap_test.jac`, `jac check lib/roadmap/tests/roadmap_ui_service_test.jac`, `jac test lib/roadmap/tests/generate_roadmap_test.jac -v`, `jac test lib/roadmap/tests/roadmap_ui_service_test.jac -v`, `jac start main.jac --faux --no_client`, and `.jac/client/node_modules/.bin/tsc.exe --noEmit` passed. Faux docs listed both `/function/generate_roadmap_for_journey` and `/function/load_roadmap` on a clean entry load.
- Current issue-fix notes: The TypeScript check required escalation because the sandbox hit Windows `EPERM` while reading `.jac/client/node_modules/typescript/bin/tsc`. Test runs emitted the existing LiteLLM remote cost-map network fallback warning and SQLite schema-drift info from local persisted test data, then completed successfully.
- Current route-declutter unit: Split the all-in-one landing/auth/assessment flow into separate route surfaces: `/` is now informational only, `/login` and `/register` are standalone auth pages that redirect to `/assessment`, `/assessment` hosts the authenticated assessment-to-roadmap journey behind `AuthGuard`, and `/roadmap/:roadmapId` remains the existing roadmap view. Auth page errors now include the thrown exception text instead of only the generic credential message.
- Current auth bugfix follow-up: Made auth storage explicitly mirror the Jac runtime `jac_token` key and separated post-login assessment recovery errors from credential failures in `LandingPage.submit_auth`, so successful `/user/register` and `/user/login` responses no longer surface as "Authentication failed" when the follow-up journey load fails.
- Current auth bugfix: Updated `lib/auth/auth_client.cl.jac` to bypass the runtime `jacLogin`/`jacSignup` response-shape assumption, post directly to the built-in auth endpoints, normalize the observed array-wrapped `{ok, data}` envelope, store `data.token` via `jacSetToken`, include the runtime `jac_token` key in recovery, and use `data.root_id` as the learner id when available.
- Current auth bugfix verification passed: `jac check lib/auth/auth_client.cl.jac`, `jac check features/landing/pages/LandingPage.cl.jac`, `jac check main.jac`, and `.jac/client/configs/node_modules/.bin/tsc.exe --noEmit`. The landing page check still emits existing dependency/type warnings unrelated to this auth fix.
- Current issue-fix: Added client auth helpers under `lib/auth/` and journey recovery storage under `lib/journey/`; updated `features/landing/pages/LandingPage.cl.jac` with register/login/logout UI, auth-gated language selection, authenticated learner id propagation, recovery behavior, explicit roadmap generation CTA, duplicate-action disabling, generation loading text, and route navigation to `/roadmap/:roadmapId`.
- Current issue-fix: Updated `services/assessment_journey.sv.jac` to load/start/submit/retry per learner and added `generate_roadmap_for_journey`, which calls `generate_roadmap`, returns the persisted `roadmap_id`, and relies on existing active-roadmap idempotency.
- Current issue-fix: Added roadmap handoff coverage in `lib/roadmap/tests/generate_roadmap_test.jac` and updated `lib/assessment/tests/assessment_journey_test.jac` to use explicit learner ids.
- Current issue-fix verification passed: `jac check lib/auth/auth_client.cl.jac`, `jac check lib/auth/auth_storage.cl.jac`, `jac check lib/auth/index.cl.jac`, `jac check lib/journey/journey_state.cl.jac`, `jac check services/assessment_journey.sv.jac`, `jac check features/landing/pages/LandingPage.cl.jac`, `jac check features/roadmap/pages/RoadmapPage.cl.jac`, `jac check main.jac`, `jac check lib/assessment/tests/assessment_journey_test.jac`, `jac test lib/assessment/tests/assessment_journey_test.jac -v`, `jac check lib/roadmap/tests/generate_roadmap_test.jac`, `jac test lib/roadmap/tests/generate_roadmap_test.jac -v`, and `.jac/client/configs/node_modules/.bin/tsc.exe --noEmit`.
- Current issue-fix test runs emitted the existing LiteLLM remote model cost-map network fallback warning and SQLite schema-drift info from persisted local test data, then completed successfully.
- Feature 11: Implemented `services/roadmap.sv.jac` read projection, `features/roadmap/pages/RoadmapPage.cl.jac`, `features/roadmap/components/RoadmapComponents.cl.jac`, and manual AppShell routing for `/roadmap/:roadmapId`.
- Feature 11 verification passed: `jac check services/roadmap.sv.jac`, `jac check main.jac`, `jac check lib/roadmap/tests/roadmap_ui_service_test.jac`, `jac test lib/roadmap/tests/roadmap_ui_service_test.jac -v`, and project-local `.jac/client/configs/node_modules/.bin/tsc.exe --noEmit`.
- Feature 11 scoped scan over `features/roadmap`, `services/roadmap.sv.jac`, `components/AppShell.cl.jac`, and `lib/types/index.ts` found no lesson generation, challenge generation, unlock, mastery, dashboard, tutor, certificate, skill-map, or recommendation wiring.
- Feature 11 Vite build verification remains blocked by the existing Windows/esbuild access-denied behavior when loading `.jac/client/configs/vite.dev.config.js`; rerunning with escalation timed out.
- Feature 10: Implemented `generate_roadmap` walker, `Roadmap`, `RoadmapWeek`, `RoadmapLesson`, `RoadmapMilestone`, roadmap status enums, containment/target/requirement graph edges, structured roadmap AI schemas, and `generate_learning_roadmap_outline`.
- Feature 10 verification passed: `jac check nodes/roadmap.jac`, `jac check lib/ai/utilities.jac`, `jac check walkers/generate_roadmap.jac`, `jac check lib/roadmap/tests/generate_roadmap_test.jac`, `jac test lib/roadmap/tests/generate_roadmap_test.jac -v`, `jac check lib/domain/index.jac`, `jac check main.jac`, and project-local `.jac/client/configs/node_modules/.bin/tsc.exe --noEmit`.
- Feature 10 test run emitted expected Jac report payloads and the existing LiteLLM network fallback warning, then completed without API keys.
- Feature 09 continuation: continuing implementation from the active landingpage-ui work. The language-template initialization path and assessment journey service are being wired before replacing the static app shell with the `/` assessment journey UI.
- Feature 08: Implemented `evaluate_assessment` walker, `lib/assessment/evaluation.jac` scoring services, `AssessmentEvaluation`, `AssessmentScore`, `QuestionResult`, `SkillSignal`, and `AssessmentEvaluationLink` domain persistence.
- Feature 08 idempotency strategy is "return existing evaluation" for the same submitted attempt; repeated evaluation returns the original evaluation record and timestamp rather than replacing it.
- Feature 08 tests at `lib/assessment/tests/evaluate_assessment_test.jac` cover all-correct, all-incorrect, mixed skill signals, short-answer normalization, idempotent repeated evaluation, and invalid assessment handling.
- Feature 07: Implemented `submit_assessment` walker, `AssessmentAttempt` and `AssessmentResponse` persistence models, validation rules, graph links, retrieval helpers, and mock tests.
- Feature 06 was implemented with a dedicated `features/assessment/` client module and `services/assessment.sv.jac` save-only service boundary.
- For Feature 06, `jac check services/assessment.sv.jac`, `jac check nodes/assessment.jac`, and `jac check main.jac` passed with UTF-8 output enabled.
- Feature 06 forbidden-scope scan over `features/assessment`, `services/assessment.sv.jac`, and `nodes/assessment.jac` found no scoring, evaluation, mastery, roadmap, or recommendation terms.
- `jac build main.jac` regenerated client files but remains blocked by the existing Windows access denial on `.jac/client/node_modules/@esbuild/win32-x64/esbuild.exe`.
- Direct Vite verification through `.jac/client/configs/node_modules/.bin/vite.exe build --config .\vite.dev.config.js` also timed out after an esbuild path access failure; leftover node/esbuild processes were stopped.
- Replaced the generated guestbook UI entry with a static GraphLearn LMS shell so the foundation matches product context without introducing domain behavior.
- Jac regenerated `.jac/client/compiled/components/AppShell.js`; Vite build and preview verification passed via the project-local Bun binary.
- `jac check main.jac` passed with the project venv Jac executable.
- Vite production build passed through `.jac/client/configs/node_modules/.bin/vite.exe`; Rollup emitted third-party `@hugeicons/core-free-icons` pure annotation warnings only.
- For `02-domain-models.md`, `jac check main.jac`, `jac check lib/domain/index.jac`, TypeScript `tsc --noEmit`, forbidden-pattern scan, and quiet Vite production build all passed.
- Jac edge endpoint syntax does not support union endpoints, so the relationship edge names are declared as untyped edge archetypes with endpoint intent documented in `edges/domain_edges.jac`.
- For `04-ai-provider-abstraction.md`, `jac check lib/ai/schemas.jac`, `jac check lib/ai/utilities.jac`, `jac check lib/ai/tests/ai_test.jac`, `jac check main.jac`, `jac test lib/ai/tests/ai_test.jac -v`, TypeScript `tsc --noEmit`, byLLM plugin/package confirmation, and forbidden-pattern scan passed.
- Full `jac test -v` is blocked by the existing client module import issue: `No module named 'frontend.cl'; 'frontend' is not a package`.
- For `05-initialize-assessment.md`, `jac check nodes/assessment.jac`, `jac check lib/ai/utilities.jac`, `jac check walkers/initialize_assessment.jac`, `jac check lib/ai/tests/assessment_test.jac`, `jac check lib/ai/schemas.jac`, `jac check lib/domain/index.jac`, `jac check main.jac`, `tsc --noEmit`, `jac test lib/ai/tests/assessment_test.jac -v`, `jac test lib/ai/tests/ai_test.jac -v`, and the no-score/no-roadmap scope scan passed.
- MockLLM test runs emitted LiteLLM network fallback warnings for the remote model cost map, then used the local backup and completed without API keys.
